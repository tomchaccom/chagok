package com.example.myapplication.core.navigation

/**
 * NavRoutes
 * - 화면 간 라우트/태그 이름을 상수로 정의합니다.
 * - Navigation Component를 사용하지 않는 프로젝트에서 프래그먼트 태그나 라우트 식별용으로 사용하세요.
 */
object NavRoutes {
    const val HOME = "home"
    const val SEARCH = "search"
    const val PROFILE = "profile"
}

