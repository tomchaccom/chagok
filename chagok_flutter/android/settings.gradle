pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include ':app'

// Flutter tooling handles additional setup via the Flutter plugin in Android Studio.
